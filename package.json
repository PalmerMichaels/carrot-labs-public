{
  "name": "carrot-labs-clean-room-cost-manager",
  "version": "1.0.0",
  "description": "Clean-room TypeScript CLI for synthetic AI provider cost management.",
  "license": "MIT",
  "bin": {
    "carrot-labs-costs": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "start": "node dist/src/cli.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  }
}
